# Ask for two grades of a student and return it's average.
firstGrade = float(input('First Grade:'))
secondGrade = float(input('Second Grade:'))
average = (firstGrade + secondGrade) / 2
print(f'\033[1mYour average is {average:.1f}')
